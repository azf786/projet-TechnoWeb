export class Client {
  constructor(public nom: string, public prenom: string, public telephone: string, public rue: string, public nomRue: string,
              public codePostal: number, public ville: string, public complement: string, public email: string ) {
  }
}
